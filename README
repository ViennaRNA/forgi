### Color the ordering of improvement for 4FAW

python examples/visualize_cg.py ~/data/cgs/4FAW_A.cg --color-gradual m9,m10,m11,m0,i2,m3,m1,m4,i0,m5,i1,i17,i3,i11,m6,i10,m7,i6,i7,i9,i8,i5,i12,i14,i13,i16,i4,i15,m8

#### Convert this to a 2D string for input into forna

python examples/ordered_elements_to_forna_colors.py ~/data/cgs/4FAW_A.cg m9,m10,m11,m0,i2,m3,m1,m4,i0,m5,i1,i17,i3,i11,m6,i10,m7,i6,i7,i9,i8,i5,i12,i14,i13,i16,i4,i15,m8

#### Display an rna structure, save it to file, trim it and view it using eog

pdb_id=3DIR; visualize_pdb.py ~/data/pdbs/${pdb_id}.pdb --batch --output ~/tmp/${pdb_id}.png; mogrify -trim ~/tmp/${pdb_id}.png; eog ~/tmp/${pdb_id}.png

#### Display coarse-grain RNA structures using [fornac](http://www.github.com/pkerpedjiev/fornac)

ernwin_go.py -i 2000 ~/data/cgs/1MFQ_A.cg --step-save 50

    ##### Some sample of structures

    name=4L81_A; num=7; find ~/projects/ernwin/ernwin_sampling/clustering/${name} -name "step*.coord" | shuf | head -n $num | xargs python examples/cg_to_fornac.py -d 25 -b 16 --sort-by mcc --names ~/data/cgs/${name}.cg > pages/index.html

    ##### Clusters of structures
    
    name=4L81_A; num=7; find ~/projects/ernwin/ernwin_sampling/clustering/${name} -name "step*.coord" | xargs python examples/cluster_kmeans.py --output-dir /tmp/clusters/${name}/ -n ${num}; find /tmp/clusters/${name} -name "*.coord" | shuf | head -n $num | xargs python examples/cg_to_fornac.py -d 25 -b 16 --sort-by mcc ~/data/cgs/${name}.cg --names > pages/index.html


